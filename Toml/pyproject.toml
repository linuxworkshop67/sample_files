[project]
name = "example_app"
version = "0.1.0"
description = "Demo Python project"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 88

